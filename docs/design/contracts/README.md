# 구현 전 계약 설계

2026-09-21 · 상태: 로컬 구현과 계약 테스트 완료 / 운영 배포·실사용 검증 전.

PRD/ADR는 제품 정책, 이 디렉터리는 데이터·호출 계약, 티켓은 작업 범위와 인수 조건을 담당한다. 티켓에 DDL/API 전체를 복제하지 않고 해당 계약을 연결한다.

에이전트 전달 명세: [공통 규칙·도구 설명·작업 가이드 관리](./agent/README.md). OAuth MCP 등록과 로컬 Edge 검증은 완료했지만 운영 배포 상태와는 구별한다. 기능 변경 시 관련 시나리오/설명/스키마/테스트를 함께 갱신한다.

## 진행 단위

| 순서 | 산출물 | 담당 | 현재 |
| --- | --- | --- | --- |
| 0 | 기존 원격/로컬 구조 대조 | #32/#44 | [조사 결과](../schema-audit-20260921.md) 작성. 운영 행 이관은 배포 게이트로 분리 |
| 1 | 공통 식별자·버전·소유권과 일일 점검 관계 | #43/#44 | [모델](./daily-review-model.md)과 도메인 migration/RPC 구현·DB 테스트 완료 |
| 2 | 문맥 조회 → 저장 → 재조회 계약 | #34/#35 | [API와 시나리오](./daily-review-api.md), OAuth 도구, 앱 조회 흐름 구현 |
| 3 | JSON Schema/반례와 SQL 제약 검증 | #34/#35/#43/#44 | 공통 도구 schema 단위 테스트와 223개 DB assertion 통과. 운영 OAuth 평가는 별도 |
| 4 | 기준점·체결·보정 계약과 decimal 계산 fixture | #32/#37/#38 | [생애주기 모델/API](./lifecycle-model-api.md), 전체 매매 생애주기 decimal fixture 구현 |
| 5 | 결정·질문·실행·공유 공개 DTO | #35/#43/#44 | 상태 전이, 체결 연결 진행도, 기능별 공유와 allowlist DTO 구현·검증 |
| 6 | 사용자 시나리오별 API/변경·불변 모델 대조 | 전체 | [24개 시나리오 점검표](./scenario-api-model-matrix.md)와 구현 상태 최종 대조 |

초기 설계 게이트를 통과한 뒤 수직 슬라이스로 구현했다. 이후 변경도 전체 계층을 미리 확장하지 않고 사용자 흐름 하나를 DB → RPC/MCP → UI → 테스트 순서로 완성한다.

## 코드 작성 전 통과 조건

- 정상/실패 JSON이 동일한 스키마로 검사되며 unknown 필드, 타인 ID, 누락 근거 등 반례가 명시돼 있다.
- 기존 테이블 재사용/신규/보존과 이관 영향이 구별돼 있다.
- 인증·직접 DB 접근·동시 수정·재시도·오래된 문맥 처리에 모순이 없다.
- 모델에서 API를, API에서 앱 화면과 다음 점검을 복원할 수 있다. 모델 추론 없이 ID/버전/관계로 이어진다.
- 검증 결과를 예상과 실제로 구분한다. fixture 검증이 RLS/트랜잭션/운영 검증을 대신하지 않는다.

## 다음 세션 시작점

[구현 현황 감사](../../tickets/implementation-audit-20260921.md)를 먼저 읽는다. 로컬 기능 구현은 완료됐으며 다음 단계는 운영 migration/Edge 배포 전 백업·legacy 데이터 대조, 배포 후 OAuth 웹·모바일 새 세션 확인, 5일 출근 파일럿이다. [ADR-0004](../../adr/0004-domain-storage-and-minimal-mutation-contract.md)의 단순화와 도메인별 저장 원칙은 계속 적용한다.
